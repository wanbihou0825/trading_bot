# Polymarket 跟单机器人依赖

# Web3 和区块链
web3>=6.0.0

# Ethereum 账户管理 (L2 API 签名必需!)
eth_account>=0.10.0

# Polymarket 官方 CLOB 客户端 (L2 认证 & 订单签名)
py-clob-client>=0.11.0

# 异步HTTP客户端
aiohttp>=3.9.0

# 数据验证
pydantic>=2.0.0

# 环境变量管理
python-dotenv>=1.0.0

# 数据处理
pandas>=2.0.0

# 类型检查支持
typing-extensions>=4.0.0

# 异步数据库
aiosqlite>=0.19.0

# ─── 生产级安全功能依赖 ───

# 重试机制 (生产级)
tenacity>=8.2.0

# 结构化日志
python-json-logger>=2.0.0

# 日志轮转 (标准库已支持，但添加额外功能)
# logging.handlers 已内置

# 测试框架 (开发依赖)
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0
pytest-mock>=3.11.0